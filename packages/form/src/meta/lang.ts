import { StringObject } from '../types'

const defaultLang: StringObject = {
  archive: 'Archive',
  delete: 'Delete',
  cancel: 'Cancel',
  edit: 'Edit',
  save: 'Save',
  requiredField: 'Field is required',
  formValidationError: 'There are remaining errors.',
}

export default defaultLang
